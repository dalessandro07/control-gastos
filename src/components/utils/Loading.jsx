import React from 'react'
import { Metronome } from '@uiball/loaders'

const Loading = () => (
  <section className="mt-12 flex w-full items-center justify-center">
    <Metronome size={45} speed={1.3} color="#FCD34D" />
  </section>
)

export default Loading
